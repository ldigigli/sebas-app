name: sebas_app
description: Aplicacion movil SEBAS para ejercitacion STEM.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  firebase_core: ^3.0.0
  flutter_bloc: ^8.1.6
  flutter_math_fork: ^0.7.2

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.7
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
